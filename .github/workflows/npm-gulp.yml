name: NodeJS with Gulp

on:
  push:
    branches: [ "master" ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 18.x

      - name: Install and Build
        run: |
          npm install
          gulp

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # If gulp puts files in a folder like 'dist', change '.' to 'dist'
          path: '.'

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
